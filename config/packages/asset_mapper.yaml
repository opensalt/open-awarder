framework:
    asset_mapper:
        # disable the polyfill for importmap
        importmap_polyfill: false

        # The paths to make available to the asset mapper.
        paths:
            - assets/
        excluded_patterns:
            - '*/assets/styles/_*.scss'
            - '*/assets/styles/**/_*.scss'

<form method="post" action="{{ path('app_email_template_duplicate', {'id': email_template.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('duplicate' ~ email_template.id) }}">
    <button class="btn btn-secondary mt-3">Duplicate</button>
</form>

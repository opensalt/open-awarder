{% extends 'base.html.twig' %}

{% block title %}EmailTemplate index{% endblock %}

{% block stylesheets %}
    <style>
        td:nth-child(2) {
            white-space:nowrap;
        }
    </style>
{% endblock %}

{% block body %}
    <h1>Email Templates</h1>

    <div {{ turbo_stream_listen('App\\Entity\\EmailTemplate') }}>
        {{ data_table(table) }}
    </div>

    <div class="mt-3">
        <a class="btn btn-secondary float-end" href="{{ path('app_email_template_new') }}">Add template</a>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Edit AchievementDefinition{% endblock %}

{% block body %}
    <h1>Edit AchievementDefinition</h1>

    {{ include('achievement_definition/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('app_achievement_definition_index') }}">Back to list</a>

    {{ include('achievement_definition/_delete_form.html.twig') }}

    <div id="edit-ob3-modal" class="modal" data-bs-backdrop="static" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Edit the Open Badge definition</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body mb-3">
                    <div id="ob3-definer"></div>
                </div>
{#                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Save changes</button>
                </div>#}
            </div>
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}AchievementDefinition{% endblock %}

{% block body %}
    <h1>AchievementDefinition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ achievement_definition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ achievement_definition.name }}</td>
            </tr>
            <tr>
                <th>Uri</th>
                <td>{{ achievement_definition.uri }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_achievement_definition_index') }}">Back to list</a>

    <a class="btn btn-primary" href="{{ path('app_achievement_definition_edit', {'id': achievement_definition.id}) }}">Edit</a>

    {{ include('achievement_definition/_delete_form.html.twig') }}
{% endblock %}

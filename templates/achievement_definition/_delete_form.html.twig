<form method="post" action="{{ path('app_achievement_definition_delete', {'id': achievement_definition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ achievement_definition.id) }}">
    <button class="btn btn-danger mt-3">Delete</button>
</form>

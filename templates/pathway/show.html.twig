{% extends 'base.html.twig' %}

{% block title %}Pathway{% endblock %}

{% block body %}
    <h1>Pathway</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pathway.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pathway.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_pathway_index') }}">Back to list</a>

    <a class="btn btn-secondary" href="{{ path('app_pathway_edit', {'id': pathway.id}) }}">Edit</a>

    {{ include('pathway/_delete_form.html.twig') }}
{% endblock %}

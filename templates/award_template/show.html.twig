{% extends 'base.html.twig' %}

{% block title %}AwardTemplate{% endblock %}

{% block body %}
    <h1>AwardTemplate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ award_template.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ award_template.name }}</td>
            </tr>
            <tr>
                <th>Template</th>
                <td>{{ award_template.template }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_award_template_index') }}">Back to list</a>

    <a class="btn btn-secondary" href="{{ path('app_award_template_edit', {'id': award_template.id}) }}">Edit</a>

    {{ include('award_template/_delete_form.html.twig') }}
{% endblock %}
